name: Cleanup GSheet Invalid Cavissima Blanc Records

on:
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Dry run (count only, no deletion)"
        required: false
        default: "false"
        type: choice
        options: ["false", "true"]

jobs:
  cleanup:
    runs-on: ubuntu-latest

    env:
      DATABASE_URL: ${{ secrets.DATABASE_URL }}
      GSHEET_CREDENTIALS_JSON: ${{ secrets.GSHEET_CREDENTIALS_JSON }}
      DRY_RUN: ${{ inputs.dry_run }}

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"
          cache: "pip"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Clean stale GSheet rows and refresh audit tab
        run: python -m src.cleanup_gsheet_invalid_records
